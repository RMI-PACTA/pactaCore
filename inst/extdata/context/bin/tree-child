#!/usr/bin/env bash

# Examples
# tree-child

n=$(tree $1 | wc -l)

# The first line of `tree` is the parent name, which
# is a user input so it's unstable
tree_unstable=1
tree_stable=$(expr $n - $tree_unstable)
tree $1 | tail --lines "$tree_stable"
