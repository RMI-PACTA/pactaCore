#!/usr/bin/env bash

source .env
export PACTA_SOURCE PACTA_INPUT PACTA_OUTPUT
mkdir -p "$PACTA_INPUT" "$PACTA_OUTPUT"
cp inst/extdata/TestPortfolio_Input_PortfolioParameters.yml "$PACTA_INPUT"
cp inst/extdata/TestPortfolio_Input.csv "$PACTA_INPUT"
